type MyPropTypes = {
    name:string;
};

type ContextType = {
    name:string;
  };

type Name = string;

type FirstName = string;
type LastName = string;
type Password = string;

type SignUpForm = {
    firstName: string;
    lastName: string;
    password: string;
  };
